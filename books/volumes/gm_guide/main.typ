#import "../../lib/veil.typ": *

#show: apply-veil-style

= GM Guide (SDK shell)

#gm[Только для мастера.]

Механический и секретный слой: `game/gm/`.

== Policy

См. `meta/SPOILER_POLICY.md` и `meta/game/QUALITY_GATE.md`.

== Adventures

Образец: `game/gm/adventures/ADV_STARFALL_SPOIL` (YAML + MD).

Шаблон Typst: `books/templates/adventure.typ`.

== Plots

`game/gm/plots/` (`PLOT_*`)

== Encounters

`game/gm/encounters/` (`ENC_*`)

== Generators

`game/gm/generators/` (`GEN_*` и MD-таблицы)

== Secrets

`game/gm/secrets/`

== Bestiary and NPC (GM use)

Design seeds: `game/bestiary/`, `game/npc/` — без reprint статов SWADE.
