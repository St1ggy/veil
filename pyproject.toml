[project]
name = "veil-tooling"
version = "0.1.0"
description = "Validators and generators for the Veil worldbuilding repo"
requires-python = ">=3.11"
dependencies = [
  "PyYAML>=6.0.3",
  "jsonschema>=4.26",
  "python-frontmatter>=1.3.0",
]

[project.optional-dependencies]
dev = ["pytest>=9.1"]
